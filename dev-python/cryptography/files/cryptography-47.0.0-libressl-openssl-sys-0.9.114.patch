--- a/build/main.rs.orig
+++ b/build/main.rs
@@ -430,6 +430,7 @@
             (4, 1, _) => ('4', '1', 'x'),
             (4, 2, _) => ('4', '2', 'x'),
             (4, 3, _) => ('4', '3', 'x'),
+            (_, _, _) => ('x', 'x', 'x'),
             _ => version_error(),
         };
 
