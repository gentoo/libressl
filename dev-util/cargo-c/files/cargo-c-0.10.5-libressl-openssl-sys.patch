--- a/build/main.rs.orig
+++ b/build/main.rs
@@ -382,6 +382,7 @@
             (3, 9, _) => ('3', '9', 'x'),
             (4, 0, 0) => ('4', '0', '0'),
             (4, 0, _) => ('4', '0', 'x'),
+            (_, _, _) => ('x', 'x', 'x'),
             _ => version_error(),
         };
 
